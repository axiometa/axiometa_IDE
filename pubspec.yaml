name: axiometa_ai_2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.9.2

flutter_icons:
  image_path: "assets/logo/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/sensor_icons/
    - assets/images/
    - assets/logo/

  fonts:
    - family: DMSANS
      fonts:
        - asset: assets/custom_fonts/DMSANS.ttf
